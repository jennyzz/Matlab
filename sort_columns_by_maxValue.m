function out = edit_columns_of_matrix(input,cutoff)
%edit_columns_of_matrix takes a matrix of values and sorts the matrix into
%descending order based upon the maximum value in the last row of every column
%
%SYNOPSIS outputMatrix = ...
%    edit_columns_of_matrix(input)
%
%INPUT  input - matrix of values to be sorted

lastLine = size(input,1);

for i=1:size(input,2)
    
    
    
end
